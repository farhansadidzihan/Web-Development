// || Normal Function ||
function avg(x, y) {
    return (x + y) / 2;
  }

// || Arrow Function ||
const sum = (p, q) => {
    return p + q;
}

let a = 25;
let b = 261;
console.log("The average of a & b is:-", avg(a, b))
console.log("The sum of a & b is:-", sum(a, b))